<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%-36.36logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- App loggers -->
    <logger name="com.example.segmentation" level="DEBUG" />

    <!-- Reduce Kafka/Confluent noise -->
    <logger name="org.apache.kafka" level="WARN" />
    <logger name="io.confluent" level="WARN" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
